# ── Credentials & keys ───────────────────────────────────────────────────────
.env
*.pem
*.key
data/clob_v2_creds.json

# ── Database ──────────────────────────────────────────────────────────────────
*.db
*.db-shm
*.db-wal
*.db.bak*
Paper_archive/

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ── Internal planning / AI notes ─────────────────────────────────────────────
tasks/
TradeBot_UserNotes.md
docs/superpowers/

# ── Local-only reports ───────────────────────────────────────────────────────
Bot_reports/

# ── Research DB (local-only, see tasks/plans/2026-05-04_d0_subplan_unified_db.md) ──
research_db/

# ── Scratch downloads (HF probes, ad-hoc dumps) ──────────────────────────────
_scratch/

# ── Script run artifacts (CSV outputs, local-only) ───────────────────────────
scripts/_out_*

# ── Bake-off scratch measurement scripts (local-only; _-prefix = throwaway) ───
bakeoff/_*.py

# ── MCP tool scratch (firecrawl dumps, playwright console/page logs) ──────────
.firecrawl/
.playwright-mcp/

# ── Local VPS diagnostic dumps ───────────────────────────────────────────────
weatherbot_vps_checkup.log
*_vps_checkup.log

# ── Snapshot rollup parquet exports (local-only; rsynced off-VPS) ────────────
snapshot_parquet/

# ── Runtime state ────────────────────────────────────────────────────────────
discovery_last_run.txt
heartbeat.json
data/dismissed_alerts.txt

# ── Logs ─────────────────────────────────────────────────────────────────────
cloudbotlogs/
logs/

# ── Debug / temp scripts ──────────────────────────────────────────────────────
debug_*.py

# ── Cloned repos ─────────────────────────────────────────────────────────────
claude-swap/
.claude/

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
scripts/_inspect_db.py
